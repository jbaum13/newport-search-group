# Build output (regenerated with `npm run build`)
dist/

# Dependencies (none today, but just in case)
node_modules/

# OS / editor junk
.DS_Store
*.log
.idea/
.vscode/

# Claude Code: share repo settings, keep machine-local ones out
.claude/settings.local.json
.claude/*.local.json

# Archived prior (Velo exec-search) build — kept on disk, not tracked
_archive-velo-execsearch/

# Finn smoke-test screenshots

# Agent smoke-test screenshots
.agent-screenshots/

# Desktop app scratch
Claude outputs/
